{
  "name": "aws-banned-profile-ask",
  "description": "PreToolUse hook that pauses for user confirmation before any AWS CLI invocation that uses the admin, stage, staging, prod, or production profile (either via --profile or AWS_PROFILE).",
  "category": "security",
  "tags": ["pre-tool-use", "bash", "aws", "iam", "confirm"],
  "source": "builtin",
  "install": { "scope": "user", "path": "~/.claude/settings.json" }
}
