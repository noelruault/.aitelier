{
  "name": "prevent-rm-rf",
  "description": "PreToolUse hook that pauses for user confirmation before any rm command with recursive + force flags. Catches the classic foot-gun where a stray variable expands to / inside `rm -rf $X`.",
  "category": "security",
  "tags": ["pre-tool-use", "bash", "rm", "confirm"],
  "source": "builtin",
  "install": { "scope": "user", "path": "~/.claude/settings.json" }
}
