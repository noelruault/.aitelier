{
  "name": "aitelier-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that serves the Aitelier SPA and proxies external-fork reads to GitHub with KV caching.",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260329.1",
    "typescript": "6.0.2",
    "wrangler": "4.78.0"
  }
}
